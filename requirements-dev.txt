# Development / CI-only dependencies.
#
# Install the runtime deps first, then these:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# (Runtime deps like fastapi live in requirements.txt; httpx is here because it
# is only needed by fastapi.testclient.TestClient during testing.)

pytest>=8.0        # test runner
ruff>=0.5          # linter + import sorter (config in pyproject.toml)
httpx>=0.27        # required by fastapi.testclient.TestClient for the API tests
pre-commit>=3.7    # runs the ruff / whitespace hooks in .pre-commit-config.yaml
