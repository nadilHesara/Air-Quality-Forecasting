# Runtime: everything src/ (data pipeline, training, serving) and the CI
# retrain workflow need.  The Phase 6 experiment-only extras (optuna, xgboost,
# statsmodels) live in requirements-experiments.txt so production stays lean.
pandas>=2.0
requests>=2.28
matplotlib>=3.7
pyarrow>=12.0
scikit-learn>=1.4
lightgbm>=4.0
shap>=0.44
joblib>=1.3
mlflow>=2.12
fastapi>=0.110
uvicorn[standard]>=0.27
streamlit>=1.31
